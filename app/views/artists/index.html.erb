<h1>All Artists</h1>

<ul>
  <% @artists.each do |artist| %>
    <ul>
      <%= link_to artist.name, artist_path(artist) %>:
      <%= "#{pluralize(artist.songs.length, 'song')}"  %>
    </ul>
  <% end %>
</ul>